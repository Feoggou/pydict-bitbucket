crashes for "have to do with"
takes each individually -- should be so, perhaps.

the related terms -- actual related terms -- appear to all have examples.
"have to do with" --- if it is found like this, it tries to download "have to do with" instead of "have-to-do-with"

related words:
-------------
must update tests in "do", etc.
must update search(), defs(), examples()
must update the printer

o) synonyms -- category for each synonym
o) refactor word definition in parser
o) doing - do + word forms (doing, done, did -- asking for either, should we automatically get all? should we redirect automatically?)
--- perhaps it's best if the user decides.

COMMANDS
o) update(word) -- show diff